import { AdaptationDetailPipe } from './adaptation-detail.pipe';

describe('AdaptationDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new AdaptationDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
