import { IsAssignedPipe } from './is-assigned.pipe';

describe('IsAssignedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsAssignedPipe();
    expect(pipe).toBeTruthy();
  });
});
