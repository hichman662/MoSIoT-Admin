import { TelemetryUnitTypePipe } from './telemetry-unit-type.pipe';

describe('TelemetryUnitTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TelemetryUnitTypePipe();
    expect(pipe).toBeTruthy();
  });
});
