import { SchemaTypePipe } from './schema-type.pipe';

describe('SchemaTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SchemaTypePipe();
    expect(pipe).toBeTruthy();
  });
});
