import { DisabilityTypePipe } from './disability-type.pipe';

describe('DisabilityTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DisabilityTypePipe();
    expect(pipe).toBeTruthy();
  });
});
