import { HazardValuePipe } from './hazard-value.pipe';

describe('HazardValuePipe', () => {
  it('create an instance', () => {
    const pipe = new HazardValuePipe();
    expect(pipe).toBeTruthy();
  });
});
