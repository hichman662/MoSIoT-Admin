import { TelemetryTypePipe } from './telemetry-type.pipe';

describe('TelemetryTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TelemetryTypePipe();
    expect(pipe).toBeTruthy();
  });
});
