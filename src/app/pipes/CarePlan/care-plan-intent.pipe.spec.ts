import { CarePlanIntentPipe } from './care-plan-intent.pipe';

describe('CarePlanIntentPipe', () => {
  it('create an instance', () => {
    const pipe = new CarePlanIntentPipe();
    expect(pipe).toBeTruthy();
  });
});
