import { PriorityTypePipe } from './priority-type.pipe';

describe('PriorityTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityTypePipe();
    expect(pipe).toBeTruthy();
  });
});
