import { AdaptationTypePipe } from './adaptation-type.pipe';

describe('AdaptationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AdaptationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
