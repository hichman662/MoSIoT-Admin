import { CareStatusPipe } from './care-status.pipe';

describe('CareStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CareStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
