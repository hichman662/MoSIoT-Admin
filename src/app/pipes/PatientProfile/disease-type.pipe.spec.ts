import { DiseaseTypePipe } from './disease-type.pipe';

describe('DiseaseTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DiseaseTypePipe();
    expect(pipe).toBeTruthy();
  });
});
