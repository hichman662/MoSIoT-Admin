import { CommandTypePipe } from './command-type.pipe';

describe('CommandTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CommandTypePipe();
    expect(pipe).toBeTruthy();
  });
});
