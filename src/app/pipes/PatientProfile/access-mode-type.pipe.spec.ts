import { AccessModeTypePipe } from './access-mode-type.pipe';

describe('AccessModeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AccessModeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
