import { TypePeriodicityPipe } from './type-periodicity.pipe';

describe('TypePeriodicityPipe', () => {
  it('create an instance', () => {
    const pipe = new TypePeriodicityPipe();
    expect(pipe).toBeTruthy();
  });
});
