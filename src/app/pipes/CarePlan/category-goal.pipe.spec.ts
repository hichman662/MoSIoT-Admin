import { CategoryGoalPipe } from './category-goal.pipe';

describe('CategoryGoalPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryGoalPipe();
    expect(pipe).toBeTruthy();
  });
});
